import { PropsWithChildren } from "react";
import Box from "@mui/material/Box";

export default function ContactsArea({ children }: PropsWithChildren) {
  return <Box className="contactsarea"></Box>;
}
