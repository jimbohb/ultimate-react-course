import { PropsWithChildren } from "react";
import Box from "@mui/material/Box";

export default function DetailsArea({ children }: PropsWithChildren) {
  return <Box className="detailsarea"></Box>;
}
